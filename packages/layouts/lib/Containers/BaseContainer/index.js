import React from 'react';
import { makeStyles } from '@mui/styles';
const useStyles = makeStyles((theme) => {
    return {
        root: {}
    };
});
export const Component = ({}) => {
    const styles = useStyles();
    return (React.createElement("div", null, "BOILERPLATE"));
};
